{
  "name": "app-overlay-extra",
  "module": "src/server/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently \"bun run server:dev\" \"bun run client:dev\"",
    "build": "bun run client:build",
    "start": "bun run server:start",
    "client:dev": "vite",
    "client:build": "vite build",
    "client:preview": "vite preview",
    "server:dev": "bun run src/server/index.ts 3001 --watch src/server",
    "server:start": "bun run src/server/index.ts",
    "check": "biome check src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "pretty-format": "^29.7.0",
    "vite": "^8.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@twurple/api": "^8.1.3",
    "@twurple/auth": "^8.1.3",
    "@twurple/chat": "^8.1.3"
  }
}